{
  "name": "az-price-comparison",
  "version": "1.0.0",
  "description": "Chrome extension to compare prices across marketplaces",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "crypto-js": "^4.2.0"
  }
}
